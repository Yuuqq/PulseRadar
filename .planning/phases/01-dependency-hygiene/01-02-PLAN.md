---
phase: 01-dependency-hygiene
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - docker/Dockerfile
  - docker/Dockerfile.mcp
  - CHANGELOG.md
  - README-EN.md
  - README.md
autonomous: true
requirements:
  - DEPS-03

must_haves:
  truths:
    - "Docker image built from docker/Dockerfile installs boto3 even though requirements.txt no longer includes it"
    - "Docker image built from docker/Dockerfile.mcp also installs boto3"
    - "CHANGELOG.md has an entry documenting the boto3 optional change"
    - "README-EN.md mentions pip install trendradar[s3] for S3 storage users"
    - "README.md mentions pip install trendradar[s3] for S3 storage users"
  artifacts:
    - path: "docker/Dockerfile"
      provides: "Updated Docker build with boto3 installation"
      contains: "boto3>=1.35.0"
    - path: "docker/Dockerfile.mcp"
      provides: "Updated MCP Docker build with boto3 installation"
      contains: "boto3>=1.35.0"
    - path: "CHANGELOG.md"
      provides: "Changelog entry for boto3 change"
      contains: "pip install trendradar[s3]"
    - path: "README-EN.md"
      provides: "Updated English install docs"
      contains: "trendradar[s3]"
    - path: "README.md"
      provides: "Updated Chinese install docs"
      contains: "trendradar[s3]"
  key_links:
    - from: "docker/Dockerfile"
      to: "requirements.txt"
      via: "COPY and pip install"
      pattern: "pip install.*requirements.txt"
    - from: "docker/Dockerfile"
      to: "boto3"
      via: "separate pip install line"
      pattern: "pip install.*boto3"
---

<objective>
Update both Dockerfiles to explicitly install boto3 after requirements.txt (since boto3 is no longer in requirements.txt), and add documentation (CHANGELOG entry + README updates) about the boto3 optional change.

Purpose: Ensure Docker deployments continue to have S3 support (per D-09, D-10, D-11), and document the breaking change for users who install from git (per D-12).
Output: Updated Dockerfiles, CHANGELOG entry, README updates in both languages.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-hygiene/01-CONTEXT.md
@.planning/phases/01-dependency-hygiene/01-RESEARCH.md
@.planning/phases/01-dependency-hygiene/01-01-SUMMARY.md

<interfaces>
<!-- Key file states the executor needs for reference. -->

From docker/Dockerfile (lines 53-54 — current pip install):
```dockerfile
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
```

From docker/Dockerfile.mcp (lines 6-7 — current pip install):
```dockerfile
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
```

CHANGELOG.md format (date-based sections with Chinese headers):
```markdown
## 2026-02-09

### Workflow UI（参数化运行 + Jobs 可观测 + 重试策略 + 模板管理）

- 详情文档：`docs/...`
- 关键变更：
  - ...
- 验证基线：`python -m pytest -q tests`（28 passed）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dockerfiles to install boto3 separately</name>
  <files>docker/Dockerfile, docker/Dockerfile.mcp</files>
  <read_first>
    - docker/Dockerfile (full file — lines 53-54 are the pip install section)
    - docker/Dockerfile.mcp (full file — lines 6-7 are the pip install section)
  </read_first>
  <action>
**Modify `docker/Dockerfile` line 54:**

Replace:
```dockerfile
RUN pip install --no-cache-dir -r requirements.txt
```

With (per D-10 — keep requirements.txt as primary source, add boto3 separately):
```dockerfile
RUN pip install --no-cache-dir -r requirements.txt && \
    pip install --no-cache-dir 'boto3>=1.35.0,<2.0.0'
```

This is a single RUN layer (joined with `&&` and `\` line continuation) to keep the Docker layer count minimal. Line 53 (`COPY requirements.txt .`) stays unchanged.

**Modify `docker/Dockerfile.mcp` line 7:**

Replace:
```dockerfile
RUN pip install --no-cache-dir -r requirements.txt
```

With (per D-11 — MCP image also gets boto3 for consistency):
```dockerfile
RUN pip install --no-cache-dir -r requirements.txt && \
    pip install --no-cache-dir 'boto3>=1.35.0,<2.0.0'
```

Line 6 (`COPY requirements.txt .`) stays unchanged.

Both Dockerfiles must:
- Keep `requirements.txt` as the primary install source (per D-10)
- Add boto3 as a SEPARATE `pip install` command chained with `&&` (per D-10)
- Use the version range `'boto3>=1.35.0,<2.0.0'` matching the pyproject.toml optional extra
- Use `--no-cache-dir` flag (matching existing pattern)
  </action>
  <verify>
    <automated>python -c "d1=open('docker/Dockerfile').read(); d2=open('docker/Dockerfile.mcp').read(); assert 'boto3>=1.35.0' in d1, 'Dockerfile missing boto3'; assert 'boto3>=1.35.0' in d2, 'Dockerfile.mcp missing boto3'; assert 'requirements.txt' in d1; assert 'requirements.txt' in d2; print('Both Dockerfiles OK')"</automated>
  </verify>
  <acceptance_criteria>
    - docker/Dockerfile contains `pip install --no-cache-dir 'boto3>=1.35.0,<2.0.0'`
    - docker/Dockerfile contains `pip install --no-cache-dir -r requirements.txt` (requirements.txt still primary)
    - docker/Dockerfile.mcp contains `pip install --no-cache-dir 'boto3>=1.35.0,<2.0.0'`
    - docker/Dockerfile.mcp contains `pip install --no-cache-dir -r requirements.txt` (requirements.txt still primary)
    - Both boto3 installs are on lines AFTER the requirements.txt install (not before)
  </acceptance_criteria>
  <done>Both Dockerfiles install boto3 separately after requirements.txt, keeping S3 available in all container deployments</done>
</task>

<task type="auto">
  <name>Task 2: Add CHANGELOG entry and update README install instructions</name>
  <files>CHANGELOG.md, README-EN.md, README.md</files>
  <read_first>
    - CHANGELOG.md (first 50 lines — to match the existing format: date-based sections with Chinese headers)
    - README-EN.md (search for "Quick Start", "install", "deployment" sections)
    - README.md (search for "快速开始", "安装", "部署" sections)
  </read_first>
  <action>
**Modify `CHANGELOG.md` — add a new date section at the TOP (after the file header):**

Insert the following block between the existing header (`# Changelog` + description line) and the first `## 2026-02-09` section:

```markdown
## 2026-04-13

### 依赖管理优化（Dependency Hygiene）

- 关键变更：
  - `boto3` 从核心依赖移至可选依赖，通过 `pip install trendradar[s3]` 安装 S3 存储支持
  - `tenacity` 版本从 `==8.5.0` 改为 `>=9.0,<10`（范围约束）
  - `requirements.txt` 改为从 `pyproject.toml` 自动生成（`uv pip compile`），补齐缺失的 `structlog` 和 `pydantic`
  - Docker 镜像（Dockerfile 和 Dockerfile.mcp）已自动包含 boto3，容器部署无需任何变更
- 影响范围：`pyproject.toml`、`requirements.txt`、`docker/Dockerfile`、`docker/Dockerfile.mcp`、`trendradar/storage/manager.py`
- 兼容性：
  - Docker 部署：完全兼容（镜像自动安装 boto3）
  - `pip install` 用户：若使用 S3 远程存储，需改用 `pip install trendradar[s3]`
  - CLI / config.yaml / 公共 API：无变化
- 验证基线：`python -m pytest -q tests`
```

**Modify `README-EN.md` — add a note about S3 optional dependency:**

Find the section that discusses Docker deployment or Quick Start. After the main deployment instructions, add a note. The exact insertion point: search for the heading that contains "Quick Start" or "Docker" and add a short note about S3 near the install/deployment instructions. If no natural insertion point exists, add a new subsection after the Quick Start section:

```markdown
> **S3 Storage Note**: If you use S3-compatible remote storage (Cloudflare R2, AWS S3, Aliyun OSS, etc.), install the S3 extra: `pip install trendradar[s3]`. Docker images include S3 support by default.
```

The exact wording and placement is at the executor's discretion, but the note MUST contain:
- The text `pip install trendradar[s3]`
- A mention that Docker images include S3 by default

**Modify `README.md` — add a note about S3 optional dependency (Chinese):**

Same approach as README-EN.md but in Chinese. Add near the install/deployment section:

```markdown
> **S3 存储说明**：如需使用 S3 兼容远程存储（Cloudflare R2、AWS S3、阿里云 OSS 等），请安装 S3 扩展：`pip install trendradar[s3]`。Docker 镜像默认已包含 S3 支持，无需额外操作。
```

The note MUST contain:
- The text `pip install trendradar[s3]`
- A mention that Docker images include S3 by default (Docker 镜像默认已包含)
  </action>
  <verify>
    <automated>python -c "c=open('CHANGELOG.md',encoding='utf-8').read(); assert 'pip install trendradar[s3]' in c; assert '2026-04-13' in c; assert 'tenacity' in c; print('CHANGELOG OK')" && python -c "r=open('README-EN.md',encoding='utf-8').read(); assert 'trendradar[s3]' in r; print('README-EN OK')" && python -c "r=open('README.md',encoding='utf-8').read(); assert 'trendradar[s3]' in r; print('README OK')"</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains a `## 2026-04-13` section
    - CHANGELOG.md section contains the string `pip install trendradar[s3]`
    - CHANGELOG.md section contains `tenacity` and `>=9.0`
    - CHANGELOG.md section contains `structlog` and `pydantic`
    - CHANGELOG.md `## 2026-04-13` section appears BEFORE `## 2026-02-09` (newest first)
    - README-EN.md contains the string `trendradar[s3]`
    - README-EN.md contains a mention of Docker images including S3 by default
    - README.md contains the string `trendradar[s3]`
    - README.md contains `Docker 镜像默认已包含` (or equivalent Chinese text about Docker including S3)
  </acceptance_criteria>
  <done>CHANGELOG documents the boto3 optional change with migration instructions, and both READMEs inform users about the [s3] extra and Docker's automatic inclusion</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker/Dockerfile -> Docker Hub / registry | Built image consumed by end users; must include all expected runtime deps |
| README/CHANGELOG -> end users | Documentation instructs users to run pip commands; must be accurate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | Docker images missing boto3 after requirements.txt change | mitigate | Task 1 adds explicit `pip install 'boto3>=1.35.0,<2.0.0'` to BOTH Dockerfiles. Verify command checks both files contain the boto3 install line. |
| T-01-07 | Spoofing | CHANGELOG/README instructs users to install wrong package | accept | The install command is the standard `pip install trendradar[s3]` pointing to the project's own pyproject.toml optional extra. No third-party package name confusion risk. |
| T-01-08 | Tampering | Docker build pulls boto3 from PyPI without hash pinning | accept | Consistent with the existing pattern where ALL dependencies in requirements.txt are pulled without hash pinning (per D-06). Adding hash pinning for boto3 alone would be inconsistent. Deferred to potential future lock-file work. |
</threat_model>

<verification>
1. `python -c "..."` checks both Dockerfiles contain boto3 install line
2. `python -c "..."` checks CHANGELOG has the new entry with required content
3. `python -c "..."` checks both READMEs mention `trendradar[s3]`
4. Full test suite still passes: `python -m pytest tests/ -q --no-header`
</verification>

<success_criteria>
- Both Dockerfiles install boto3 separately after requirements.txt
- CHANGELOG.md has a 2026-04-13 entry documenting the change
- README-EN.md and README.md both mention `pip install trendradar[s3]`
- All existing tests still pass (no regressions from doc/Docker changes)
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-hygiene/01-02-SUMMARY.md`
</output>
