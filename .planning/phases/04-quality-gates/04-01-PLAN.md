---
phase: 04-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "Running `ruff check .` exits clean with I, F, E, UP, B, SIM, RUF rules enabled"
    - "Ruff configuration exists in pyproject.toml with specified rule sets"
    - "Line length is set to 100 characters"
  artifacts:
    - path: "pyproject.toml"
      provides: "Ruff configuration section"
      contains: "[tool.ruff]"
  key_links:
    - from: "pyproject.toml"
      to: "ruff CLI"
      via: "configuration file"
      pattern: "\\[tool\\.ruff\\]"
---

<objective>
Configure Ruff linting in pyproject.toml with rule sets I, F, E, UP, B, SIM, RUF and verify the codebase passes all checks.

Purpose: Establish automated code quality checks to prevent regression in the newly-refactored codebase.
Output: pyproject.toml with [tool.ruff] configuration, zero lint violations across 119 Python files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-gates/04-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Ruff configuration to pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Add [tool.ruff] configuration section to pyproject.toml after the existing [tool.coverage.xml] section. Configuration must include:

- target-version = "py310" (matches project's requires-python)
- line-length = 100 (per RESEARCH.md recommendation)
- exclude = [".git", ".venv", "__pycache__", "*.egg-info", "output", "_image"]

Add [tool.ruff.lint] section with:
- select = ["I", "F", "E", "UP", "B", "SIM", "RUF"] (exact rule sets from QUAL-01)
- ignore = [] (start with no ignores)

Add [tool.ruff.lint.isort] section with:
- known-first-party = ["trendradar", "mcp_server"]

Use the exact configuration from RESEARCH.md "Ruff Configuration for Python 3.10+ Project" section.
  </action>
  <verify>
    <automated>ruff check . --select I,F,E,UP,B,SIM,RUF</automated>
  </verify>
  <done>
- pyproject.toml contains [tool.ruff], [tool.ruff.lint], and [tool.ruff.lint.isort] sections
- Configuration matches RESEARCH.md template exactly
- `ruff check . --select I,F,E,UP,B,SIM,RUF` exits with code 0 (no violations)
- All 119 Python files pass lint checks
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced - this is pure tooling configuration.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | .pre-commit-config.yaml (future) | mitigate | Pin hook versions in Plan 03, use official ruff-pre-commit repo |
| T-04-02 | Tampering | Ruff binary | accept | Using globally installed ruff 0.14.13, official Astral release |
</threat_model>

<verification>
Run `ruff check . --select I,F,E,UP,B,SIM,RUF` and verify exit code 0.
Inspect pyproject.toml to confirm [tool.ruff] section exists with all required fields.
</verification>

<success_criteria>
- pyproject.toml contains complete Ruff configuration
- `ruff check . --select I,F,E,UP,B,SIM,RUF` exits clean (0 violations)
- Configuration matches RESEARCH.md template
- CLI, config.yaml loading, and Docker deployment still work identically
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-gates/04-01-SUMMARY.md`
</output>
