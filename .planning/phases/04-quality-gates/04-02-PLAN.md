---
phase: 04-quality-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - "**/*.py"
autonomous: true
requirements:
  - QUAL-02

must_haves:
  truths:
    - "Running `ruff format --check .` exits clean (all files formatted)"
    - "All 119 Python files follow consistent Ruff formatting"
    - "Tests still pass after formatting"
  artifacts:
    - path: "trendradar/**/*.py"
      provides: "Formatted Python source files"
      min_lines: 1
    - path: "mcp_server/**/*.py"
      provides: "Formatted Python source files"
      min_lines: 1
  key_links:
    - from: "ruff format"
      to: "*.py files"
      via: "automated formatting"
      pattern: "\\.py$"
---

<objective>
Format the entire codebase with `ruff format .` to establish consistent code style across all 119 Python files.

Purpose: One-time formatting operation to bring the codebase into compliance with Ruff's opinionated style, enabling format checks in pre-commit hooks.
Output: All Python files formatted consistently, `ruff format --check .` exits clean, tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-gates/04-RESEARCH.md
@.planning/phases/04-quality-gates/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Format entire codebase with Ruff</name>
  <files>**/*.py</files>
  <action>
Run `ruff format .` to format all Python files in the repository. This is a one-time operation that will reformat ~138 files according to Ruff's opinionated style.

RESEARCH.md confirms the codebase already passes lint checks (0 violations), so formatting should not introduce any lint errors. However, formatting will change whitespace, line breaks, and import ordering across the codebase.

After formatting, verify:
1. `ruff format --check .` exits with code 0 (all files formatted)
2. `ruff check . --select I,F,E,UP,B,SIM,RUF` still exits with code 0 (no new lint violations)
3. `pytest tests/ --cov=trendradar --cov=mcp_server --cov-fail-under=27` still passes (formatting didn't break tests)

If any test failures occur, investigate and fix before proceeding. Formatting should be purely cosmetic and not affect behavior.
  </action>
  <verify>
    <automated>ruff format --check . && ruff check . --select I,F,E,UP,B,SIM,RUF && pytest tests/ --cov=trendradar --cov=mcp_server --cov-fail-under=27 -x</automated>
  </verify>
  <done>
- `ruff format --check .` exits with code 0 (no files need formatting)
- `ruff check . --select I,F,E,UP,B,SIM,RUF` exits with code 0 (no lint violations)
- All tests pass with coverage >= 27%
- CLI (`python -m trendradar`) still works
- Git diff shows formatting changes only (no logic changes)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced - this is pure code formatting.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | Formatted code | accept | Formatting is deterministic and reversible, git history preserved |
</threat_model>

<verification>
Run `ruff format --check .` and verify exit code 0.
Run `pytest tests/` and verify all tests pass.
Run `python -m trendradar --help` and verify CLI still works.
</verification>

<success_criteria>
- All Python files formatted consistently
- `ruff format --check .` exits clean
- All tests pass (coverage >= 27%)
- CLI, config.yaml loading, and Docker deployment still work identically
- Git diff shows only formatting changes (whitespace, line breaks, import order)
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-gates/04-02-SUMMARY.md`
</output>
